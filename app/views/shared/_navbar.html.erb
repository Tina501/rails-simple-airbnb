<nav class="navbar navbar-expand bg-body-tertiary">
  <div class="container">
    <%# <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample02" aria-controls="navbarsExample02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button> %>

    <div class="collapse navbar-collapse d-flex justify-content-end">
      <%= form_with url: flats_path, method: :get, local: true, class: "d-flex align-items-center" do %>
        <div class="d-flex align-items-center justify-content-center">
          <%= text_field_tag :query, params[:query], placeholder: "Search for a flat...", class: "search-input" %>
          <%= submit_tag "Search", class: "search-button" %>
        </div>

        <div class="d-flex align-items-center justify-content-center ms-2">
          <a href="<%= new_flat_path %>" class="btn btn-outline-primary" style="padding: 10px 15px">Add Flat</a>
        </div>
      <% end %>
    </div>
  </div>
</nav>
